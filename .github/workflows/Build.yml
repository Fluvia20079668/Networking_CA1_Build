name: Build and Deploy to ECR and EKS

on:
  push:
    branches:
      - main

env:
  AWS_REGION: eu-north-1
  IMAGE_REPO: my-simple-app
  ECR_REGISTRY: 332427206046.dkr.ecr.eu-north-1.amazonaws.com

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout source code
        uses: actions/checkout@v4

      # 2️⃣ Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 4️⃣ Login to Amazon ECR
      - name: Login to Amazon ECR
        run: |
          echo "Logging in to ECR: ${{ env.ECR_REGISTRY }}"
          aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
            docker login --username AWS --password-stdin ${{ env.ECR_REGISTRY }}

      # 5️⃣ Build and push Docker image
      - name: Build and push Docker image
        run: |
          IMAGE_TAG=${{ github.sha }}
          IMAGE_NAME="${{ env.ECR_REGISTRY }}/${{ env.IMAGE_REPO }}:$IMAGE_TAG"
          echo "Building and pushing image: $IMAGE_NAME"
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            -t "$IMAGE_NAME" \
            --push .
          echo "IMAGE_URI=$IMAGE_NAME" >> $GITHUB_ENV

      # 6️⃣ Set up kubectl
      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.30.0'

      # 7️⃣ Configure kubeconfig for EKS cluster
      - name: Configure kubeconfig for EKS
        run: |
          aws eks update-kubeconfig --name my-eks-cluster --region ${{ env.AWS_REGION }}
          kubectl get nodes

      # 8️⃣ Deploy to EKS with new image
      - name: Deploy to EKS
        run: |
          echo "Updating deployment with image: $IMAGE_URI"
          kubectl set image deployment/${{ env.IMAGE_REPO }} ${{ env.IMAGE_REPO }}=$IMAGE_URI
          echo "Waiting for rollout..."
          kubectl rollout status deployment/${{ env.IMAGE_REPO }}

      # 9️⃣ Verify pods
      - name: Verify pods
        run: kubectl get pods -o wide

      # 🔟 Get LoadBalancer URL
      - name: Get LoadBalancer URL
        id: get-url
        run: |
          URL=$(kubectl get svc ${{ env.IMAGE_REPO }}-service -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
          echo "Live URL: http://$URL"
          echo "url=http://$URL" >> $GITHUB_OUTPUT

      # 11️⃣ Show live application link
      - name: Show live application link
        run: |
          echo "✅ Your app is live at:"
          echo "${{ steps.get-url.outputs.url }}"

      # 12️⃣ Cleanup
      - name: Cleanup
        run: docker image prune -af
